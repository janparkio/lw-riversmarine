type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "riversmarine",
  site_description: "Rivers Marine",
  site_domain: "https://riversmarine.net",
};
